{
  "name": "moc-iot-docs",
  "version": "1.0.0",
  "engines": {
    "node": ">=18.x",
    "python": ">=3.8"
  },
  "scripts": {
    "install": "echo 'Installing dependencies' && pip install -r requirements.txt",
    "build": "mkdocs build",
    "dev": "mkdocs serve"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.39.0"
  },
  "devDependencies": {}
}